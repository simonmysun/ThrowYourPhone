{
  "name": "ThrowYourPhone",
  "version": "1.0.0",
  "description": "ThrowYourPhone",
  "scripts": {
    "build": "cp src/index.html dist/index.html; esbuild src/main.js --bundle --minify --sourcemap --outdir=dist",
    "dev": "npm run build -- --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "MY Sun <simonmysun@gmail.com> (https://simonmysun.github.io/)",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "0.25.0"
  }
}